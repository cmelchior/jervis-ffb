name: Check TourPlay Icons

on:
  workflow_call:
    inputs:
      commit_count:
        required: true
        type: string

permissions:
  contents: read

jobs:
  check-tourplay-icons:
    runs-on: ubuntu-latest
    env:
      JERVIS_COMMIT_COUNT: ${{ inputs.commit_count }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          submodules: "recursive"

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '21'

      - name: Setup Gradle and task/dependency caching
        uses: gradle/actions/setup-gradle@v3
        with:
          cache-read-only: false

      - name: Check TourPlay icon mapping
        run: ./gradlew checkTourPlayIcons
